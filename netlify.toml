# Netlify configuration for SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build]
  publish = "dist"
  command = "npm run build"

[[plugins]]
  package = "@netlify/plugin-lighthouse"

# Explicitly excluding cloudinary plugin
# [[plugins]]
#  package = "netlify-plugin-cloudinary"
